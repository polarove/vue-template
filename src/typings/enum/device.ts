export enum Platform {
  Mobile = 'mobile',
  Desktop = 'desktop',
  NotSupported = 'NotSupported',
}

export enum OS {
  iOS = 'iOS',
  Android = 'Android',
  Windows = 'Windows',
  MacOS = 'MacOS',
  NotSupported = 'NotSupported',
}
